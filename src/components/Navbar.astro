---
---
<nav class="navbar">
  <div class="nav-inner">
    <div class="nav-brand">
      <a href="/" class="nav-logo">더프라이빗택스</a>
      <span class="nav-sub">세무사 허혜주</span>
    </div>

    <button class="hamburger" id="hamburger" aria-label="메뉴" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>

    <ul class="nav-menu" id="nav-menu">
      <li class="nav-search-mobile"><a href="/search/" class="nav-link">검색</a></li>
      <li class="has-dropdown">
        <button class="nav-link dropdown-trigger">세금 이야기<span class="arrow">▾</span></button>
        <ul class="dropdown">
          <li><a href="/category/chwideuk/">취득세</a></li>
          <li><a href="/category/yangdo/">양도소득세</a></li>
          <li><a href="/category/jongbu/">종합부동산세</a></li>
          <li><a href="/category/jeungyeo/">증여세</a></li>
          <li><a href="/category/sangsok/">상속세</a></li>
          <li><a href="/category/etc/">기타</a></li>
        </ul>
      </li>
      <li class="has-dropdown">
        <button class="nav-link dropdown-trigger">자료실<span class="arrow">▾</span></button>
        <ul class="dropdown">
          <li><a href="/resources/#forms">서식</a></li>
          <li><a href="/resources/#policy">정책·보도자료</a></li>
          <li><a href="/category/guide/">이렇게 준비하세요</a></li>
          <li><a href="/resources/regulated-areas/">규제지역 조회</a></li>
        </ul>
      </li>
      <li><a href="/consult/" class="nav-link">상담 안내</a></li>
      <li><a href="/about/" class="nav-link">세무사 소개</a></li>
      <li class="nav-search-desktop">
        <a href="/search/" class="nav-link search-icon" aria-label="검색">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
        </a>
      </li>
    </ul>
  </div>
</nav>

<style>
  .navbar {
    background: var(--cream);
    position: sticky;
    top: 0;
    z-index: 100;
    border-bottom: 1px solid rgba(59, 42, 30, 0.13);
  }
  .nav-inner {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 clamp(1.5rem, 4vw, 3.5rem);
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 80px;
  }
  .nav-brand {
    display: flex;
    align-items: baseline;
    gap: 0.65rem;
  }
  .nav-logo {
    font-family: var(--font-bookk);
    font-size: 1.35rem;
    font-weight: 300;
    letter-spacing: -0.08em;
    color: var(--dark-brown);
  }
  .nav-sub {
    font-size: 0.75rem;
    color: #9A745D;
  }

  /* Search visibility */
  .nav-search-mobile { display: none; }
  .search-icon {
    display: flex;
    align-items: center;
    padding: 0.5rem;
  }

  /* Menu */
  .nav-menu {
    display: flex;
    list-style: none;
    gap: 0.75rem;
    align-items: center;
  }
  .nav-link {
    background: none;
    border: none;
    font-family: 'SUIT Variable', 'Pretendard', sans-serif;
    font-size: 0.88rem;
    font-weight: 500;
    color: var(--dark-brown);
    padding: 0.5rem 0.35rem;
    cursor: pointer;
    transition: color 0.15s;
  }
  .nav-link:hover {
    background: transparent;
    color: var(--terracotta);
  }

  /* Arrow indicator */
  .arrow {
    font-size: 0.7rem;
    margin-left: 0.3rem;
  }

  /* Dropdown */
  .has-dropdown {
    position: relative;
  }
  .dropdown {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    background: var(--paper);
    min-width: 165px;
    padding: 0.65rem 0;
    border: 1px solid var(--line-light);
    border-top: 2px solid var(--terracotta);
    box-shadow: 0 10px 24px rgba(44, 33, 23, 0.08);
    list-style: none;
  }
  .has-dropdown:hover .dropdown {
    display: block;
  }
  .dropdown li a {
    display: block;
    padding: 0.5rem 1rem;
    font-size: 0.82rem;
    color: var(--dark-brown);
    transition: color 0.15s;
  }
  .dropdown li a:hover {
    background: transparent;
    color: var(--terracotta);
  }

  /* Hamburger */
  .hamburger {
    display: none;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
    flex-direction: column;
    gap: 5px;
  }
  .hamburger span {
    display: block;
    width: 22px;
    height: 2px;
    background: var(--dark-brown);
    border-radius: 0;
    transition: transform 0.2s, opacity 0.2s;
  }

  /* Mobile */
  @media (max-width: 768px) {
    .hamburger {
      display: flex;
    }
    .nav-search-mobile { display: list-item; }
    .nav-search-desktop { display: none; }
    .nav-menu {
      display: none;
      position: absolute;
      top: 80px;
      left: 0;
      right: 0;
      background: var(--cream);
      flex-direction: column;
      padding: 0.5rem 0;
      gap: 0;
      box-shadow: 0 10px 24px rgba(44,33,23,0.08);
    }
    .nav-menu.open {
      display: flex;
    }
    .nav-menu > li {
      border-bottom: 1px solid var(--line-light);
    }
    .nav-menu > li:last-child {
      border-bottom: none;
    }
    .nav-link {
      display: flex;
      align-items: center;
      justify-content: space-between;
      width: 100%;
      text-align: left;
      padding: 0.85rem 1.5rem;
      border-radius: 0;
    }
    .has-dropdown {
      position: static;
    }
    .has-dropdown:hover .dropdown {
      display: none;
    }
    .has-dropdown.open .dropdown {
      display: flex;
      flex-direction: column;
    }
    .dropdown {
      position: static;
      box-shadow: none;
      border-radius: 0;
      padding: 0.25rem 0 0.25rem 2rem;
      background: transparent;
      border-left: 1px solid var(--terracotta);
      margin-left: 1.5rem;
    }
    .dropdown li a {
      padding: 0.4rem 1rem;
      font-size: 0.8rem;
    }
  }
</style>

<script>
  const hamburger = document.getElementById('hamburger')!;
  const menu = document.getElementById('nav-menu')!;

  hamburger.addEventListener('click', () => {
    const isOpen = menu.classList.toggle('open');
    hamburger.setAttribute('aria-expanded', String(isOpen));
  });

  function updateArrow(el: Element, open: boolean) {
    const arrow = el.querySelector('.arrow');
    if (arrow) arrow.textContent = open ? '▴' : '▾';
  }

  document.querySelectorAll('.dropdown-trigger').forEach((btn) => {
    btn.addEventListener('click', () => {
      if (window.innerWidth <= 768) {
        const parent = btn.closest('.has-dropdown')!;
        document.querySelectorAll('.has-dropdown.open').forEach((el) => {
          if (el !== parent) {
            el.classList.remove('open');
            updateArrow(el, false);
          }
        });
        const isOpen = parent.classList.toggle('open');
        updateArrow(parent, isOpen);
      }
    });
  });
</script>
